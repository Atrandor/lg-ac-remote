name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:   # manueller "Run workflow"-Button im Actions-Tab

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: JDK 17 einrichten
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle einrichten (mit Cache)
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Debug-APK bauen
        run: gradle assembleDebug

      - name: Keystore wiederherstellen
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > /tmp/ks.jks

      - name: Release-APK bauen (signiert)
        run: gradle assembleRelease

      - name: APK als Artifact hochladen
        uses: actions/upload-artifact@v4
        with:
          name: LgAcRemote-apk
          path: app/build/outputs/apk/release/app-release.apk
          retention-days: 30
